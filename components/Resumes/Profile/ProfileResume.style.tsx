import { createStyles } from "@mantine/core";

const useProfileResumeStyles = createStyles((theme) => ({
    wrapper: {
        background: "rgba(222, 222, 222, 0.7)",
        boxShadow: "0px 0px 10px rgba(0, 0, 0, 0.35)",
    }
}));

export default useProfileResumeStyles

